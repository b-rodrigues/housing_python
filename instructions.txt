pyenv install 3.12.1

# switch to project directory and set up local python version

pyenv local 3.12.1
# puts a .python_version file in that directory

# create a virtualenv
pyenv virtualenv project_name

# activate the environment for local use
pyenv local project_name
pyenv activate project_name

# see and cite:https://realpython.com/intro-to-pyenv/#local 

# install packages
pip install plotnine polars openpyxl

python -m pip freeze > requirements.txt
